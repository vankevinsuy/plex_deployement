services:
  plex:
    build:
      context: .
      dockerfile: plex_arm64.dockerfile
    container_name: plex
    network_mode: host
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Montreal
      - VERSION=docker
    volumes:
      - ./plex/config:/config
      - ./plex/data/photos:/data/photos
      - ./plex/data/movies:/data/movies
      - ./plex/data/animated_movies:/data/animated_movies
      - ./plex/data/series:/data/series
    restart: always
    ports:
      - 32400:32400